# Virtual environments
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local configuration and secrets
.env
.env.local

# Runtime data (SQLite database, uploaded books, generated artifacts)
data/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
# Superpowers SDD scratch (progress ledgers, review packages)
.superpowers/
/docs/superpowers/

# Agent-only repository notes
/CLAUDE.md
/frontend/CLAUDE.md
/frontend/AGENTS.md

# Source textbooks (PDFs). Out of scope per ADR-012..016: the app imports
# structured book JSON, never a PDF. Kept locally, never committed.
# Anchored to the repository root: unanchored, this also swallowed the console's
# /books route, which then went untracked and unscanned by Tailwind.
/books/
